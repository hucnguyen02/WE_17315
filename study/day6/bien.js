const a = 10;
const b = 20;

function sum (a, b){
    // kiểm tra 
    if (typeof a !== "number" || typeof a !== "number"); return -1;
    // xử lý 
    const result = a + b;
    // kết quả
    return result;
}

console.log(sum(a, b));